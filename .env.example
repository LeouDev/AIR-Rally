# Optional. Used for absolute URLs in metadata (Open Graph, etc). Defaults to
# https://airrally.app if unset — no keys are required to run the app locally.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
